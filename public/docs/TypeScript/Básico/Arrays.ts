// Declarando uma array
let lista = [1, "string", false];

// Propriedades
let tamanho: number = lista.length; // Retorna o tamanho da array

// Métodos
lista.removeAt(1); // Remove um item da lista em um índice específico

// Útil
if (item in lista) {} // Verifica se um item está em uma lista ou não