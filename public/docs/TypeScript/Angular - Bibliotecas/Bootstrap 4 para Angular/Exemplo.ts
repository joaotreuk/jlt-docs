// Comando para instalação
//npm install ngx-bootstrap --save

// Importando para uso um determinado módulo, no arquivo app.module.ts
import { ModalModule } from 'ngx-bootstrap/modal';

// No "imports"
ModalModule.forRoot() // Importando o componente modal