/*-------------------- MÉTODOS --------------------*/

// Gerar um número aleatório que seja menor que o número passado
int numero = Random.Shared.Next(numeroMaximo);
int numero = Random.Shared.Next(numeroMinimo, numeroMaximo); // Número aleatório que esteja entre o range