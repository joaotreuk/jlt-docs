// Cria um novo elemento de imagem
Image imagem = Image.GetInstance(uri); // Passa um objeto URI do sistema

// Métodos
imagem.ScalePercent(13f); // Escala a imagem para um tamanho passado
imagem.SetAbsolutePosition(370f, 685f); // Define uma posição absoluta da imagem no documento [Horizontal, Vertical]