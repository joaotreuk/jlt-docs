uses System.DateUtils

// Manipular data
StartOfTheMonth(Date); // Pega a data do primeiro dia do mês da data específicada
EndOfTheMonth(Date); // Pega a data do último dia do mês da data específicada