// Chars
caracter: Char; // Declaração
caracter := 'J'; // Atribuição

{ -------------------- MÉTODOS -------------------- }

// Incrementar o caracter
Inc(caracter); // Incrementa o caracter em 1
Inc(caracter, -3); // Incrementar o caracter em um valor passado

{ ----- CONTROLE BINÁRIO ----- }
// Cria um caracter apartir de seu número representante, 13 = Quebra de linha
Char(13);

// Obter o número representante de um caracter
Ord('A');