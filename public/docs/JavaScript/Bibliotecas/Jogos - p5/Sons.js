/* O navegador não vai permitir arquivos estáticos serem rodados no navegador,
  então tem que fazer host para essa lib funcionar */
// O navegador não vai deixar tocar a música logo no começo, o usuário precisa fazer uma interação primeiro
//<script src="https://cdnjs.cloudflare.com/ajax/libs/p5.js/1.5.0/addons/p5.sound.min.js"></script>

// Carregar som para variável
let raquetada = loadSound("raquetada.mp3");