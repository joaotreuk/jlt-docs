// Executa um código ou expressão
// Evitar usar porque é perigoso e lento
let resultado = eval("5 * 2");
objeto.funcao = eval("(function() {return 1})"); // É possível usalo para converter um texto para uma função

// Deletar um elemento
delete objeto.Idade; // Deletar uma propriedade de um objeto
delete lista[1]; // Deletar um item de uma matriz