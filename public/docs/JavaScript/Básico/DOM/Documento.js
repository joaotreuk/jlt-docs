document.getElementById("TxtExemplo") = TxtExemplo // A ID de cada elemento se torna uma variável automaticamente

document.write(NovoDocumento) // Escrever ou sobrescrever no document

// Pega o nome da tag do elemento com o foco atual da tela
document.activeElement.tagName // retorna DIV por exemplo